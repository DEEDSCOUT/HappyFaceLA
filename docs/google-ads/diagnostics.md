# Google Ads Diagnostics — Happy Faces LA

_This file is generated by `tools/google_ads_mcp/server.py` via the
`google_ads_generate_diagnostics_report` MCP tool. It is a placeholder until
credentials are loaded into `tools/google_ads_mcp/.env.local` and the tool runs
against client account `469-912-0105` (via MCC `634-051-0052`)._

> **Blocker (2026-05-20):** Developer token access level is **Test Account**.
> Live API calls against `469-912-0105` will fail until Basic or Explorer access
> is approved from the MCC (`634-051-0052`) API Center. OAuth scaffold may
> proceed; hold all diagnostic runs until access is upgraded.

The report covers:

- Account, currency, time zone, conversion-tracking settings
- All campaigns with status / serving status / channel / sub-type / bid strategy / budget / optimization score
- Kids Party Face Painting deep-dive (asset groups, signals, locations, conversion goals)
- Conversion actions (status, category, type, origin, primary_for_goal, include_in_conversions_metric, counting, attribution)
- Customer- and campaign-level conversion goals
- **Conversion-tracking-incomplete diagnosis** — answers whether `Submit lead form` / `generate_lead` is configured correctly
- PMax asset groups (text, image, video) with performance labels
- Search themes & audience signals
- Campaign location targeting
- Risks, UI-only items, recommended next actions
- Last 30 days of change events

Secrets are run through a redactor before this file is written. The previous
read-only placeholder at `docs/google-ads-diagnostics.md` has been superseded
by this location.
